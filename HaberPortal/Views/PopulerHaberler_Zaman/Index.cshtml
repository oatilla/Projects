@model IEnumerable<HaberPortal.Models.tbl_PortalHaber_PopulerHaberler_Zaman>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Zaman)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_PortalHaber_Site.Site_Adi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Zaman)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_PortalHaber_Site.Site_Adi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PHZ_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.PHZ_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PHZ_Id })
        </td>
    </tr>
}

</table>
