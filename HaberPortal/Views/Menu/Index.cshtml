@model IEnumerable<HaberPortal.Models.tbl_PortalHaber_Menu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MenuAciklama)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MenuLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sıra)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MenuAciklama)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MenuLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sıra)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
