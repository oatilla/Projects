@model IEnumerable<HaberPortal.Models.tbl_PortalHaber_kategori>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Kid }) |
            @Html.ActionLink("Details", "Details", new { id=item.Kid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Kid })
        </td>
    </tr>
}

</table>
